import { createPeerTypeModule } from "../base";

/** Marketing peer focus areas. Understanding is loaded via marketing-understanding layer. */
export const marketingModule = createPeerTypeModule("Marketing", [
  "Campaign planning",
  "Content drafting",
  "Demand generation",
  "Brand positioning",
]);
